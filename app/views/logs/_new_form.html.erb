<%= form_for(@log) do |f| %>
  <%= hidden_field_tag :cosme_id, @cosme.id %>
  <div class="row">
    <div class="col-md-8 col-md-offset-1">
      <div class="field">
        <%= f.text_area :content, placeholder: "コスメメモ" %>
      </div>
    </div>
    <div class="col-md-3">
      <%= f.submit "感想を追加する", class: "btn btn-primary" %>
    </div>
  </div>
<% end %>
